import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  calendar: {
    // borderWidth: 1,
    // borderColor: 'gray',
    // height: 50,
    // backgroundColor: 'red',
    
  },
});

export default styles;
